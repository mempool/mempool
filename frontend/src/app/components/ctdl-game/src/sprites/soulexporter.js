const w = 21;
const h = 39;

export default {
  right: {
    idle: [
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 1 * w, y: 0 * h, w, h },
      { x: 2 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 1 * h, w, h },
      { x: 1 * w, y: 1 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 0 * h, w, h },
      { x: 1 * w, y: 0 * h, w, h },
      { x: 2 * w, y: 0 * h, w, h },
      { x: 0 * w, y: 1 * h, w, h },
      { x: 1 * w, y: 1 * h, w, h }
    ]
  }
};